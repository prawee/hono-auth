{
  "name": "hono-auth",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "docker compose up -d"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.3",
    "drizzle-seed": "^0.3.1",
    "hono": "^4.8.5",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}